# GitHub 部署操作步骤

**当前状态**: ✅ Git 仓库已初始化,文件已提交

---

## 📋 接下来你需要做的事情

### 步骤 1: 创建 GitHub 仓库 (5分钟)

1. **打开浏览器**,访问: https://github.com/new

2. **填写仓库信息**:
   - **Repository name**: `scan` (或 `smartscan`)
   - **Description**: `SmartScan - Fast, secure QR code scanner`
   - **Public** ← 必须选择公开
   - ❌ **不要**勾选 "Add a README file"
   - ❌ **不要**勾选 "Add .gitignore"
   - ❌ **不要**选择 "Choose a license"

3. **点击** "Create repository" 按钮

4. **记下你的 GitHub 用户名** (后面会用到)

---

### 步骤 2: 推送代码到 GitHub (5分钟)

创建仓库后,GitHub 会显示一个页面,**复制你的仓库 URL**。

然后在终端执行以下命令:

```bash
# 进入项目目录
cd /Users/fancw/StudioProjects/scan

# 添加远程仓库 (替换 [你的用户名] 为实际的 GitHub 用户名)
git remote add origin https://github.com/[你的用户名]/scan.git

# 推送代码
git branch -M main
git push -u origin main
```

**示例** (假设你的 GitHub 用户名是 `fanei`):
```bash
git remote add origin https://github.com/fanei/scan.git
git branch -M main
git push -u origin main
```

**如果需要登录**:
- 用户名: 你的 GitHub 用户名
- 密码: 使用 Personal Access Token (不是密码)
  - 生成 Token: https://github.com/settings/tokens
  - 选择 "Generate new token (classic)"
  - 勾选 `repo` 权限
  - 复制生成的 Token 作为密码使用

---

### 步骤 3: 启用 GitHub Pages (3分钟)

1. **访问仓库设置页面**:
   ```
   https://github.com/[你的用户名]/scan/settings/pages
   ```

2. **配置 GitHub Pages**:
   - 找到 "Build and deployment" 部分
   - **Source**: 选择 "Deploy from a branch"
   - **Branch**: 选择 `main` (不是 master)
   - **Folder**: 选择 `/docs` (不是 / (root))
   - 点击 **"Save"** 按钮

3. **等待部署**:
   - 页面顶部会显示: "Your site is ready to be published at..."
   - 等待 1-5 分钟
   - 刷新页面,会显示: "Your site is live at..."

---

### 步骤 4: 验证部署 (2分钟)

部署完成后,访问以下 URL 测试:

**首页**:
```
https://[你的用户名].github.io/scan/
```

**隐私政策** (这是最重要的):
```
https://[你的用户名].github.io/scan/privacy.html
```

**测试内容**:
- ✅ 页面能正常打开
- ✅ 中英文切换正常
- ✅ 隐私政策内容完整
- ✅ 联系邮箱显示为 faneizn@gmail.com

---

## 🎯 获取隐私政策 URL

部署成功后,你的**隐私政策 URL** 是:

```
https://[你的用户名].github.io/scan/privacy.html
```

**这个 URL 需要保存下来**,后面在 Google Play Console 中会用到!

---

## 📝 常见问题

### Q1: 推送时要求输入密码怎么办?

**A**: 不要使用 GitHub 密码,需要使用 Personal Access Token:

1. 访问: https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 点击 "Generate token"
5. **复制 Token** (只显示一次,务必保存)
6. 在终端输入密码时,粘贴这个 Token

### Q2: 404 错误,页面打不开?

**A**: 可能的原因:

1. **等待时间不够**: GitHub Pages 需要 1-5 分钟部署,请耐心等待
2. **URL 错误**: 确保 URL 格式正确,包含 `/scan/` 路径
3. **分支/文件夹配置错误**: 检查 Settings → Pages 配置
4. **文件未推送**: 确保 `git push` 成功执行

### Q3: 如何查看部署状态?

**A**: 访问仓库的 Actions 页面:
```
https://github.com/[你的用户名]/scan/actions
```

可以看到部署进度和日志。

### Q4: 如何更新隐私政策?

**A**: 修改文件后重新推送:

```bash
cd /Users/fancw/StudioProjects/scan
git add docs/privacy.html
git commit -m "Update privacy policy"
git push
```

等待 1-5 分钟,GitHub Pages 会自动更新。

---

## ✅ 完成后的检查清单

部署完成后,请确认:

- [ ] GitHub 仓库已创建
- [ ] 代码已成功推送
- [ ] GitHub Pages 已启用
- [ ] 首页可以访问
- [ ] 隐私政策页面可以访问
- [ ] 中英文切换正常
- [ ] 已保存隐私政策 URL

---

## 🚀 下一步

完成 GitHub Pages 部署后:

1. ✅ 保存隐私政策 URL
2. ✅ 继续准备其他发布材料:
   - 应用截图 (4-6张)
   - 数据安全表单
   - 内容分级问卷
3. ✅ 在 Google Play Console 中填写隐私政策 URL

---

## 💡 提示

- **URL 格式很重要**: 确保是 `https://` 开头,包含完整路径
- **仓库必须公开**: 免费的 GitHub Pages 只支持公开仓库
- **文件夹必须是 /docs**: 我们的文件都在 docs 文件夹中
- **耐心等待**: 首次部署可能需要 5-10 分钟

---

**准备好了吗?** 按照上面的步骤操作,完成后告诉我你的 GitHub 用户名,我会帮你验证 URL!🚀
